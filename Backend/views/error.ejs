<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error - Quiz App</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
  </head>
  <body class="bg-gray-100 min-h-screen">
    <div class="min-h-screen flex items-center justify-center py-12 px-4">
      <div class="max-w-md w-full">
        <!-- Error Card -->
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
          <!-- Error Header with gradient -->
          <div
            class="bg-gradient-to-r from-red-500 to-pink-600 p-8 text-center"
          >
            <div
              class="w-20 h-20 mx-auto mb-4 bg-white/20 rounded-full flex items-center justify-center"
            >
              <i class="fas fa-exclamation-triangle text-3xl text-white"></i>
            </div>
            <h1 class="text-3xl font-bold text-white mb-2">Oops!</h1>
            <p class="text-white/90 text-lg">Something went wrong</p>
          </div>

          <!-- Error Content -->
          <div class="p-8 text-center">
            <div class="mb-6">
              <p class="text-gray-700 text-lg leading-relaxed">
                <%= message %>
              </p>
            </div>

            <!-- Action Buttons -->
            <div class="space-y-3">
              <a
                href="/leaderboard"
                class="w-full inline-flex items-center justify-center px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-lg hover:from-blue-600 hover:to-purple-700 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
              >
                <i class="fas fa-trophy mr-2"></i>
                Back to Leaderboard
              </a>

              <a
                href="/"
                class="w-full inline-flex items-center justify-center px-6 py-3 bg-gray-100 text-gray-700 font-semibold rounded-lg hover:bg-gray-200 transition-all duration-200"
              >
                <i class="fas fa-home mr-2"></i>
                Go to Home
              </a>
            </div>

            <!-- Additional Help -->
            <div class="mt-8 pt-6 border-t border-gray-200">
              <p class="text-sm text-gray-500 mb-3">
                Need help? Try refreshing the page or contact support.
              </p>
              <button
                onclick="window.location.reload()"
                class="inline-flex items-center text-sm text-blue-600 hover:text-blue-800 transition-colors"
              >
                <i class="fas fa-redo mr-1"></i>
                Refresh Page
              </button>
            </div>
          </div>
        </div>

        <!-- Optional: Error Code Display (if you want to show error codes) -->
        <!-- Uncomment and modify if needed
        <div class="mt-4 text-center">
          <p class="text-sm text-gray-400">
            Error Code: <span class="font-mono">ERR_<%= errorCode || '500' %></span>
          </p>
        </div>
        -->
      </div>
    </div>

    <!-- Optional: Add some subtle animation -->
    <script>
      // Add a subtle fade-in animation
      document.addEventListener("DOMContentLoaded", function () {
        const errorCard = document.querySelector(".bg-white.rounded-2xl");
        errorCard.style.opacity = "0";
        errorCard.style.transform = "translateY(20px)";
        errorCard.style.transition = "opacity 0.5s ease, transform 0.5s ease";

        setTimeout(() => {
          errorCard.style.opacity = "1";
          errorCard.style.transform = "translateY(0)";
        }, 100);
      });
    </script>
  </body>
</html>
